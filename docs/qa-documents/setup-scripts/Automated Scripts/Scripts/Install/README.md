# Deprecated Folder

This folder previously contained VM-origin install scripts.

Those scripts were removed from the repository working set.

Use git history if historical content is required.

Use the maintained scripts in:

- ../../../install/

Do not run legacy scripts from the archive unless required for historical comparison.
